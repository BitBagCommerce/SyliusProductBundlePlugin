{% if product.variants.empty() or product.simple and not sylius_inventory_is_available(product.variants.first) %}
    {% include '@SyliusShop/Product/Show/_outOfStock.html.twig' %}
{% else %}
    {{ render(url('bitbag_sulius_product_bundle_shop_partial_cart_add_product_bundle', {'template': '@BitBagSyliusProductBundlePlugin/Shop/Product/_addProductBundleToCart.html.twig', 'productId': product.id})) }}
{% endif %}
